
use warnings;
use File::Basename qw(dirname);
use Cwd  qw(abs_path);
use lib '/home/bernardo/projects/elab/www/';

use area qw( areaOfCircle );

$areaOfFirstCircle = area.areaOfCircle(5);
print("The area of the circle is: $areaOfFirstCircle <br>")
